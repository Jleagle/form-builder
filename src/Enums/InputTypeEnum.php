<?php
namespace Jleagle\FormBuilder\Enums;

class InputTypeEnum extends BaseEnum
{
  const BUTTON         = 'button'; // Button
  const CHECKBOX       = 'checkbox';
  const COLOUR         = 'color'; // Default
  const DATALIST       = 'datalist';
  const DATE           = 'date'; // Default
  const DATETIME       = 'datetime'; // Default
  const DATETIME_LOCAL = 'datetime-local'; // Default
  const EMAIL          = 'email'; // Default
  const FILE           = 'file'; // Default
  const HIDDEN         = 'hidden'; // Default
  const IMAGE          = 'image'; // Button
  const MONTH          = 'month'; // Default
  const NUMBER         = 'number'; // Default
  const PASSWORD       = 'password'; // Default
  const RADIO          = 'radio';
  const RANGE          = 'range'; // Default
  const RESET          = 'reset'; // Button
  const SEARCH         = 'search'; // Default
  const SELECT         = 'select';
  const SUBMIT         = 'submit'; // Button
  const TEL            = 'tel'; // Default
  const TEXT           = 'text'; // Default
  const TEXTAREA       = 'textarea';
  const TIME           = 'time'; // Default
  const URL            = 'url'; // Default
  const WEEK           = 'week'; // Default
}
